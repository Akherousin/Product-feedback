const paths = {
  home() {
    return '/';
  },

  createRequest() {
    return `/new`;
  },

  //   postShow(topicSlug: string, postId: string) {
  //     return `/topics/${topicSlug}/posts/${postId}`;
  //   },
};

export default paths;
